# better-sqlite3 ships prebuilt Node-API addons in its tarball, so nothing here
# has to compile — and a build that never runs an install script is a build a
# stranger can reproduce.
ignore-scripts=true
